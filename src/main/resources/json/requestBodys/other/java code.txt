Hay que passarlo en una sola linia
para que se ejecute jaca hace falta classe principal con método main

"public class Challenge {
    public static void main(String args[]) { //algunos compilers permiten datos inputs
        //Parametros a testear propuestos por el creador del challenge
        int x = 3; int y = 5;

        int sum = new Challenge().doSum(x,y);

        //Lo que el compiler devuelve:
        System.out.print(sum);
   }

   //Metodo propuesto por el alumno:
   public int doSum(int a, int b){
        return a+b;
   }

}"